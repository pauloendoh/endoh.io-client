import { Button } from "@material-ui/core";
import styled from "styled-components";

const S = {
  RateButton: styled(Button)(({ theme }) => ({
    position: "relative",
    right: 8,
  })),
};

export default S;
