import { MenuItem } from "@material-ui/core";
import styled from "styled-components";

const S = {
  MenuItem: styled(MenuItem)`
    display: flex;
    align-items: center;
    gap: 4;
    padding-top: 0;
    padding-bottom: 0;
  `,
};

export default S;
